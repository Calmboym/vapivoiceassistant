# infrastructure/docker

Reserved for production Docker assets beyond the per-app Dockerfiles
already in apps/api/Dockerfile and apps/web/Dockerfile (e.g. a
multi-stage production compose override, or CI build configs). Not
populated yet.

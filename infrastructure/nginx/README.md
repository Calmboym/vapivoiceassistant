# infrastructure/nginx

Reserved for a reverse-proxy config (TLS termination, routing
web/api under one domain) for a non-containerized-platform production
deployment. Not populated yet — most PaaS targets (Fly.io, Render,
Railway, ECS) won't need this at all.

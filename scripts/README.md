# scripts/

Reserved for one-off operational scripts — e.g. the `scripts/setup_vapi.py`
described in the original spec's §51, which will create/update Vapi tools
and the assistant once Phase 5 (Vapi integration) starts. Nothing here yet.

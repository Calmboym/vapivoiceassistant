# packages/shared

Reserved for code shared between apps/web and apps/api that isn't purely
type definitions (e.g. shared constants, validation logic ported to both
TS and Python). Not populated yet — nothing in Phases 1–3 needed it.

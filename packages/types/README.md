# packages/types

Reserved for TypeScript types generated from (or mirrored from) the
FastAPI/Pydantic schemas in apps/api/app/schemas/, so the frontend and
backend can't drift silently. Not populated yet — worth setting up once
apps/web has more than the one placeholder page (e.g. via
`openapi-typescript` against the backend's auto-generated OpenAPI schema).

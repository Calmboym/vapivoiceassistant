# tests/integration

Reserved for API -> PostgreSQL / API -> Redis / API -> Stripe-test-mode
integration tests, once `pip install`-ing pytest + a running Postgres/Redis
is possible. apps/api/tests/test_core_logic.py currently covers the
dependency-free core logic only.

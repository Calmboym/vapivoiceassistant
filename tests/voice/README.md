# tests/voice

Reserved for the voice conversation test suite from the original spec's
§56 (e.g. "customer says 'book it' with no quoted flight -> agent must not
book"). Needs the Vapi assistant/tools (Phase 5) to exist first.

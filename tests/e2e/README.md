# tests/e2e

Reserved for Playwright end-to-end tests (search -> book -> lookup ->
cancel -> modify -> human transfer) against the running web + api stack.
Not started — needs apps/web to have real booking pages first (Phase 9+).
